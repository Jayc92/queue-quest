# Archive

Historical prototype files.

## `queue-quest-prototype.html`

The original single-file Queue Quest prototype (React + Tailwind CDN + in-browser Babel).
Kept for reference only. **The canonical Queue Quest app lives in this repo root** (Vite + React + TypeScript).

Do not extend or bug-fix this prototype. All active development should target the Vite project.
